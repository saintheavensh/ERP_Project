node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright (H15 gap b — e2e UI walk). Screenshots/traces are regenerated on
# every run; the spec + config are committed, the binary artifacts are not.
/test-results
/playwright-report
/blob-report
/playwright/.cache
